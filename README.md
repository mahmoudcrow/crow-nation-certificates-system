# 🎓 Crow Nation Certificates System - Plugin Documentation

**الإصدار:** 1.0  
**المؤلف:** Mahmoud Moustafa  
**التاريخ:** 29 ديسمبر 2025

---

## 📋 ما هو هذا البلجن؟

نظام إدارة الشهادات الاحترافي للمؤسسات والمراكز التدريبية. يتيح إنشاء وإدارة والتحقق من الشهادات رقمياً مع توليد رموز QR تلقائياً.

---

## ✨ المميزات الرئيسية

### 🎨 **التصميم الحديث**
- تصميم Metronic Inspired (احترافي وعصري)
- ألوان متناسقة وجذابة
- Responsive Design (يعمل على الموبايل والتابلت)
- CSS Variables للتخصيص السهل
- Smooth Animations و Transitions

### 🔒 **الأمان العالي**
- Nonce verification على جميع النماذج
- SQL Injection Protection مع `wpdb->prepare()`
- XSS Protection مع `esc_html()`, `esc_url()`, `esc_attr()`
- CSRF Protection لجميع الطلبات
- حذف آمن للصور عند حذف الشهادات

### 📊 **إدارة متقدمة**
- إضافة وتعديل وحذف الشهادات
- بحث متقدم (السيريال + الاسم + العنوان)
- تصدير CSV للشهادات
- استيراد CSV للبيانات الجماعية
- عرض جميع الشهادات في جدول منظم

### 🤖 **QR Code التلقائي**
- توليد QR Code تلقائي لكل شهادة
- الربط مع صفحة التحقق
- عرض QR في الإدارة والـ Frontend

### 🔍 **التحقق من الشهادات**
- صفحة تحقق عامة (Shortcode)
- عرض تفاصيل الشهادة الكاملة
- حالة الشهادة (نشطة/منتهية/ملغاة)
- عرض صورة الشهادة ورمز QR
- واجهة سهلة وجميلة

### 📧 **REST API**
- نقطة API للتحقق من الشهادات: `/wp-json/crow-certificates/v1/verify?serial=SERIAL`
- JSON Response مع جميع البيانات

### 📈 **الإحصائيات**
- لوحة تحليلات شاملة
- عدد الشهادات الكلي
- توزيع الحالات (نشطة/منتهية/ملغاة)
- نسب مئوية بصرية

### 🔄 **التحديثات التلقائية**
- نظام GitHub Auto-Update
- تحديثات آمنة من المستودع
- إخطارات التحديث في لوحة التحكم

---

## 📁 هيكل الملفات

```
crow-nation-certificates/
├── crow-nation-certificates.php     # ملف التفعيل الرئيسي
├── uninstall.php                    # ملف إلغاء التثبيت
├── admin/
│   ├── admin-page.php               # صفحة الإدارة
│   └── analytics-page.php           # صفحة الإحصائيات
├── includes/
│   ├── create-table.php             # إنشاء الجدول
│   ├── api.php                      # REST API
│   ├── certificate-functions.php    # الدوال المساعدة
│   └── github-updater.php           # نظام التحديث
├── public/
│   └── shortcode-display.php        # صفحة التحقق
├── assets/
│   ├── style.css                    # الأنماط
│   └── script.js                    # JavaScript
└── TESTING_GUIDE.md                 # دليل الاختبار
```

---

## 🚀 كيفية الاستخدام

### 1️⃣ **التثبيت**
- انسخ المجلد إلى `wp-content/plugins/`
- فعّل البلجن من لوحة التحكم
- ستظهر قائمة "Certificates" في الجانب الأيسر

### 2️⃣ **إضافة شهادة**
- انتقل إلى `Certificates > Certificates`
- اضغط "➕ إضافة شهادة جديدة"
- املأ النموذج وحفظ
- QR Code سيُوَلّد تلقائياً

### 3️⃣ **التحقق من الشهادة**
- أضف shortcode في صفحة: `[crow_certificate_checker]`
- سيظهر نموذج بحث للعملاء
- ادخل السيريال والتحقق فوري

### 4️⃣ **الاستيراد/التصدير**
- **تصدير**: اضغط "📥 تصدير CSV"
- **استيراد**: اختر ملف CSV واضغط "📤 استيراد CSV"

---

## 📊 جدول قاعدة البيانات

```sql
CREATE TABLE wp_crow_certificates (
    id mediumint(9) AUTO_INCREMENT PRIMARY KEY,
    serial varchar(100) UNIQUE NOT NULL,
    name varchar(255) NOT NULL,
    title varchar(255) NOT NULL,
    reason text,
    issue_date date,
    expiry_date date,
    status varchar(50),
    certificate_image varchar(255),
    qr_code_url varchar(500)
);
```

---

## 🎯 الدوال المتاحة

### الدوال الأساسية (في certificate-functions.php)

```php
// الحصول على شهادة برقمها
crow_get_certificate_by_serial($serial)

// الحصول على شهادة برقمها الداخلي
crow_get_certificate_by_id($id)

// التحقق من صحة الشهادة
crow_is_certificate_valid($cert)

// توليد رابط QR Code
crow_generate_qr_code_url($serial)

// تنسيق البيانات للعرض
crow_format_certificate($cert)

// البحث عن الشهادات
crow_search_certificates($search_term)

// الحصول على الإحصائيات
crow_get_certificate_stats()

// تصدير CSV
crow_export_certificates_csv()
```

---

## 🌐 REST API

### نقطة التحقق
```
GET /wp-json/crow-certificates/v1/verify?serial=SERIAL-001
```

**Response (JSON)**:
```json
{
    "found": true,
    "serial": "SERIAL-001",
    "name": "أحمد محمد",
    "title": "شهادة الإتقان",
    "reason": "برنامج تطوير الويب",
    "issue_date": "2025-01-01",
    "expiry_date": "2026-01-01",
    "status": "active",
    "certificate_image": "https://...",
    "qr_code_url": "https://..."
}
```

---

## 🎨 التخصيص

### تغيير الألوان
في `assets/style.css`:
```css
:root {
    --primary: #0099CC;           /* اللون الأساسي */
    --success: #1BC47D;           /* لون النجاح */
    --danger: #F1416C;            /* لون الخطأ */
    /* وغيرها... */
}
```

### تغيير الخطوط
في `assets/style.css`:
```css
body {
    font-family: 'اسم الخط', sans-serif;
}
```

---

## 🔐 متطلبات الأمان

✅ **متطلبات مستوفاة:**
- Nonce verification على جميع النماذج
- SQL Injection protection
- XSS protection
- CSRF protection
- Capability checks (manage_options)
- Secure file upload handling
- Safe image deletion

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا تظهر الشهادات في الجدول
- **الحل**: تأكد من تفعيل البلجن وأن الجدول موجود في قاعدة البيانات
- **فحص**: `wp_crow_certificates` يجب أن تكون موجودة

### المشكلة: QR Code لا يعمل
- **الحل**: تأكد من اتصالك بالإنترنت (Google Charts API)
- **بديل**: يمكن استخدام API أخرى

### المشكلة: الصور لا تُحمّل
- **الحل**: تأكد من أذونات المجلد `/uploads/`
- **فحص**: وجود `WP_CONTENT_DIR/uploads/`

### المشكلة: Shortcode لا يظهر
- **الحل**: تأكد من كتابة: `[crow_certificate_checker]` بدون مسافات
- **فحص**: تفعيل البلجن

---

## 📞 الدعم والتطوير

**للإبلاغ عن أخطاء:**
1. اكتب خطوات إعادة إنتاج المشكلة
2. أرفق صورة أو فيديو
3. تحقق من debug.log

**للطلب ميزة جديدة:**
- التواصل مع المطور
- وصف الميزة المطلوبة بالتفصيل

---

## 📜 الترخيص

هذا البلجن مرخص تحت GPL v2 (نفس ترخيص WordPress)

---

## 🎉 شكر خاص

تم بناء هذا البلجن بأحدث معايير الأمان والتصميم.  
شكراً لاستخدام Crow Nation Certificates System!

---

**آخر تحديث:** 29 ديسمبر 2025  
**الإصدار:** 1.0.0
